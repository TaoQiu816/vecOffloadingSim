[pytest]
addopts = -m "not nightly and not optional_feature"
markers =
    optional_feature: optional/non-core feature tests that are not required for main training regression
    nightly: long-running or white-box checks reserved for nightly runs
